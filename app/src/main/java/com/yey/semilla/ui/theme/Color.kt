package com.yey.semilla.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Primarios (Modo Claro)
val PrimaryBlue = Color(0xFF007AFF)       // Azul Vibrante: para botones principales y acentos
val SecondaryGreen = Color(0xFF34C759)    // Verde Fresco: para confirmaciones o tareas completadas
val BackgroundLight = Color(0xFFF9F9F9)   // Fondo Muy Claro: base limpia
val SurfaceLight = Color(0xFFFFFFFF)      // Superficie Blanca: para tarjetas y elementos elevados

// Colores de Texto y Tono
val TextDark = Color(0xFF1C1C1E)          // Negro Oscuro: para texto principal
val TextMedium = Color(0xFF8E8E93)        // Gris Medio: para subtítulos o texto secundario
val ErrorRed = Color(0xFFFF3B30)          // Rojo para errores o alta prioridad

// Colores para Prioridades/Etiquetas (Opcional)
val HighPriority = Color(0xFFFF9500)      // Naranja (Alertas)
val MediumPriority = Color(0xFF5AC8FA)    // Azul Claro (Normal)